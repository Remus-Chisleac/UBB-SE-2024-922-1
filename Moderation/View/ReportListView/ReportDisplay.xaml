<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Moderation.ReportListView.ReportDisplay"
             >
    <!--<StackLayout Margin="20">
        <Label Text="Report" FontSize="20" FontAttributes="Bold"/>
        <StackLayout Orientation="Horizontal">
            <Label Text="Report ID:" FontSize="16" Margin="0,4,10,0"/>
            <Label Text="{Binding ReportId}" FontSize="16" Margin="0,4,0,0"/>
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="User ID:" FontSize="16" Margin="0,4,10,0"/>
            <Label Text="{Binding UserId}" FontSize="16" Margin="0,4,0,0"/>
        </StackLayout>
        <Entry Text="{Binding Message}" Placeholder="Enter message..." Margin="0,4,0,0"/>
        <StackLayout Orientation="Horizontal" Margin="0,10,0,0">
            <Button Text="Warning" Clicked="OnWarningClicked"/>
            <Button Text="Mute" Clicked="OnMuteClicked"/>
            <Button Text="Ban" Clicked="OnBanClicked"/>
            <Button Text="Dismiss" Clicked="OnDismissClicked"/>
        </StackLayout>
    </StackLayout>-->
</ContentView>